export type User = {
  id: string;
  username: string;
  email: string;
  company: string;
  monthly_salary: number;
  avatar_url?: string;
  first_name?: string;
  last_name?: string;
  points_balance?: number;
};